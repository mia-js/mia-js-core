var ErrorHandler = require('mia-js-core/node_modules/errorHandler');

describe("External modules", function () {

    it("must be available", function (next) {
        expect(ErrorHandler).toBeDefined();
        next();
    });
});
